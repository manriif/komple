local.name=Komple
local.description=Downloads, verifies, installs and exposes native development tools (compilers, SDKs, CLI utilities) directly from your build, through a composable shell environment and a native (C) project abstraction.
local.tags=komple,native,toolchain,c,build-tools